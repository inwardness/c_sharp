
static void Main()
{

    Console.WriteLine("Enter a massive:");
    int x = 3;
    int y = 3;
    int[,] arr = new int[x, y];
    for (int i = 0; i < x; ++i)
    {
        for (int j = 0; j < y; j++)
        {
            Console.WriteLine("mas[" + i + "," + j + "]: ");
            arr[i, j] = int.Parse(Console.ReadLine());
        }
    }

    for (int i = 0; i < x; ++i)
    {
        for (int j = 0; j < y; j++)
        {
            Console.Write(arr[i, j] + " ");
        }
        Console.WriteLine("\n");
    }

    Console.WriteLine();

    int public static reverse(int arr, int x, int y)
    {
        for (int i = 0; i < arr.Length; i++)
        {
            
        }
        Console.WriteLine("Read");
        Console.Read()
        
    }
}